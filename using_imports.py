import random  # Import the built-in random module for generating random numbers
import hello_world  # Import the custom hello_world module (assumes it's in the same directory or installed)

number = random.randint(0, 10)  # Generate a random integer between 0 and 10 (inclusive)

print(number)  # Output the generated number to the console
